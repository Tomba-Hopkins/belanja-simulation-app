<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Profile</title>
</head>
<body>
    
    <h1>Profile</h1>
    <ul>
        <li>ID: <%= id %></li>
        <li>Name: <%= user.name %></li>
        <li>NIK: <%= user.nik %></li>
    </ul>

    <% if (id == 2) { %>
     <h2>Harga nasgor 50k</h2>
    <% } %>

    <% if (id == 3) { %>
     <h3 style="color: whitesmoke;">di id 50 keknya</h3>
    <% } %>

    <% if (id == 50) { %>
     <h3 style="text-align: center;">U R so cool bro congrats</h3>
    <% } %>
    
</body>
</html>